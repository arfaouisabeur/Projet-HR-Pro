<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/20"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="edu.RhPro.controllers.employe.ProfilEmployeController"
      spacing="20"
      alignment="TOP_CENTER"
      style="-fx-background-color:white; -fx-padding:30;"
      prefWidth="600"
      prefHeight="500">

    <!-- Affichage -->
    <VBox spacing="10"
          style="-fx-background-color:#f4f4f4; -fx-padding:20; -fx-background-radius:10;">

        <Label text="👤 Informations Employé"
               style="-fx-font-size:18px; -fx-font-weight:bold;" />

        <GridPane hgap="10" vgap="10">

            <Label text="Nom :" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
            <Label fx:id="nomLabel" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

            <Label text="Prénom :" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
            <Label fx:id="prenomLabel" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

            <Label text="Email :" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
            <Label fx:id="emailLabel" GridPane.rowIndex="2" GridPane.columnIndex="1"/>

            <Label text="Téléphone :" GridPane.rowIndex="3" GridPane.columnIndex="0"/>
            <Label fx:id="telLabel" GridPane.rowIndex="3" GridPane.columnIndex="1"/>

        </GridPane>
    </VBox>

    <!-- Modification -->
    <VBox spacing="10">

        <Label text="✏ Modifier vos informations"
               style="-fx-font-size:16px; -fx-font-weight:bold;" />

        <TextField fx:id="nomField" promptText="Nom"/>
        <TextField fx:id="prenomField" promptText="Prénom"/>
        <TextField fx:id="emailField" promptText="Email"/>
        <TextField fx:id="telField" promptText="Téléphone"/>

        <Button text="💾 Enregistrer"
                onAction="#updateProfil"
                style="-fx-background-color:#2c7be5; -fx-text-fill:white; -fx-font-weight:bold;" />

    </VBox>

</VBox>
